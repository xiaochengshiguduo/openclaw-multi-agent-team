# Workspace Template

These files are copied into generated OpenClaw Agent workspaces.

Private files such as real `MEMORY.md`, `USER.md`, local `TOOLS.md`, sessions, auth profiles, and OpenClaw runtime config must stay local and must not be committed.
