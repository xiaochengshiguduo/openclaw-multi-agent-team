# docs role

This directory contains the reusable OpenClaw role template for the `docs` long-lived Agent.

- `SOUL.md`: role identity and responsibilities.
- `AGENTS.md`: collaboration protocol, boundaries, and output rules.

Do not place private memory, user data, tokens, sessions, or machine-specific runtime state here.
